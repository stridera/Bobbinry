# Corkboard

Visual board interface for organizing and reordering content cards
(scenes, beats, scraps) with drag-and-drop.

- Provides a `customViews` view that the shell mounts as a project workspace.
- Stores card layout in entity data alongside any other bobbin's content.
- No external services or AI calls.
