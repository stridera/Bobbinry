# Discord Role Manager

Sync Discord server roles based on Bobbinry subscription tiers. When a
subscription is created, upgraded, or canceled, the matching Discord role
is added or removed automatically.

- Project-scoped settings panel under `shell.rightPanel`.
- Requires a Discord bot token with `manage_roles` permission.
- Tier → role mapping is configured per project in the panel.
