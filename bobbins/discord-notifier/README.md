# Discord Notifier

Announce chapter publishes to Discord channels via webhooks. Map subscription
tiers to specific channels for targeted notifications (e.g. early-access tier
gets a heads-up before public).

- Project-scoped settings panel under `shell.rightPanel`.
- Triggered by chapter publish events from the API.
- Requires a Discord webhook URL per channel; configured per project.
